apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-servers

resources:
  - namespace.yaml
  - filesystem-mcp.yaml
  - database-mcp.yaml
  - code-analysis-mcp.yaml

commonLabels:
  app.kubernetes.io/part-of: agent-gateway-platform
  app.kubernetes.io/managed-by: kustomize
